/**
 * One reading measure and one gutter for every surface that sets text: the
 * home page, the answer column, the library reader. Keeping them identical
 * means the text edge does not move between screens.
 */
export const READING_COLUMN = 'w-full max-w-180'
export const READING_GUTTER = 'px-5 sm:px-12'
