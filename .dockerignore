# Python
venv/
.venv/
**/__pycache__/
**/*.pyc
**/*.pyo
.env

# Web app (not needed in the API build context)
web/node_modules/
web/dist/
web/.vite/

# Sample corpus — uploaded to S3 by seed_documents.py, not needed in the image
data/

# Caddy config is bind-mounted, not baked into an image
Caddyfile

# Git / IDE / docs
.git/
.gitignore
**/*.md
docs/
assets/
