# Weekly dependency update PRs, grouped so one PR per ecosystem lands on Monday
# rather than a dozen. Security advisories still open their own PR immediately.

version: 2
updates:
  # One entry covers every file under requirements/. Separate entries per
  # file produced duplicate PRs. Inputs are *.in and the compiled locks are
  # *.txt: Dependabot only recompiles a lock with pip-compile when it finds
  # that pairing. With any other names it edits lock lines one at a time with
  # no resolver, which is how #173 broke pydantic-core (see #177).
  - package-ecosystem: pip
    directory: /requirements
    schedule:
      interval: weekly
      day: monday
    groups:
      python:
        patterns: ["*"]
    labels: [dependencies]

  - package-ecosystem: npm
    directory: /web
    schedule:
      interval: weekly
      day: monday
    groups:
      web-minor-and-patch:
        update-types: [minor, patch]
    labels: [dependencies]
    ignore:
      # typescript-eslint pins typescript to <6.1.0, so npm ci refuses the
      # 7.x line. Drop this once typescript-eslint supports TypeScript 7.
      - dependency-name: typescript
        versions: [">=7.0.0"]

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    groups:
      actions:
        patterns: ["*"]
    labels: [dependencies, ci]

  - package-ecosystem: docker
    directories: [/, /web]
    schedule:
      interval: weekly
      day: monday
    groups:
      docker:
        patterns: ["*"]
    labels: [dependencies, infra]
