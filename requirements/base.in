# Dependencies every role needs. Not installed on its own; api.in and
# ingest.in include it with -r.

# OpenAI — embeddings and chat completions via sourcebook/rag/llm.py
openai

# MongoDB — passages, conversations, projects, caches, query logs
pymongo

# Environment variable management — loads .env into os.environ
python-dotenv
